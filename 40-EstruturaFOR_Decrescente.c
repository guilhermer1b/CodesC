#include <stdio.h>

/*

    Estrutura de repetição FOR decrescente

*/

int main(){
    int i = 10;

    for(i = 10; i >= 0; i--) // Decremento ( -- ) || Incremento ( ++ )
        printf("%d ", i);
    return 0;
}